#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

echo "==> Running eslint --fix"
SKIP_GRANULAR_DIFF=true ./node_modules/.bin/eslint --fix .

echo "==> Running prettier --write"
./node_modules/.bin/prettier --write --cache --cache-strategy metadata .
